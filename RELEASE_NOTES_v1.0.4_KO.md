# Arfni v1.0.4 릴리즈 노트

**릴리즈 일자:** 2026-08-05

v1.0.4는 영속 SSH 세션, 자동 재접속, 실제 PTY 기반 터미널, 터미널 경로를 인식하는 SFTP 탐색, Docker 배포 안정화 기능을 추가해 원격 개발 경험을 개선합니다.

---

## 새로운 기능

### 영속 SSH 세션

- 서버별 영속 세션 설정 추가
- 설정이 활성화되면 원격 셸을 tmux로 실행
- SSH 연결이 끊긴 뒤 기존 tmux 세션에 다시 연결
- 로컬 네트워크 또는 SSH 연결이 일시적으로 끊겨도 원격 작업 유지
- 원격 서버에 tmux가 없으면 일반 로그인 셸로 자동 전환

### SSH 자동 재접속

- 예기치 않은 SSH 연결 종료 감지
- 원격 연결 장애만 자동 재접속
- 3초부터 60초까지 지수 백오프 적용
- 자동 재접속은 최대 6회 시도
- 사용자가 직접 종료한 세션이나 닫은 탭은 다시 열지 않음
- 재접속 시 마지막으로 사용한 터미널 크기 복원
- 재접속 예약, 재시도, 중단 상태를 터미널에 표시

### SSH 터미널 개선

- 실제 PTY 기반으로 터미널 재구성
- Codex, vim, htop 등 TUI 애플리케이션의 인터랙티브 동작 개선
- Ctrl+C, Tab 자동완성, 이스케이프 시퀀스, 붙여넣기 동작 보존
- 재접속 후 남아 있는 터미널 화면 모드 초기화
- xterm.js의 마우스 동작을 유지하면서 tmux 스크롤백 지원
- 라우트 이동과 탭 전환 후에도 터미널 세션 유지

### 터미널 연동 SFTP 탐색

- SSH 터미널 탭별 현재 작업 디렉터리를 SFTP가 자동으로 따라감
- 터미널 제목과 셸 프롬프트에서 경로 감지
- tmux가 터미널 제목 이벤트를 전달하지 않는 환경 지원
- Codex 또는 다른 TUI 실행 중에도 마지막으로 확인된 경로 유지
- SFTP 패널에 터미널 경로와 따라가기 상태 표시
- SFTP 패널을 열면 터미널 경로 따라가기를 기본 활성화

---

## 개선 사항

### 서버 설정

- 서버 설정 화면에 영속 세션 옵션 추가
- 데이터베이스 migration 007을 통해 설정 저장
- 기존 서버의 기본값은 비활성화로 유지

### SFTP 사용자 경험

- 감지된 터미널 경로 표시
- 따라가기/일시 정지 토글 추가
- 따라가기 상태를 localStorage에 영구 저장하지 않도록 변경
- 따라가기를 다시 켜면 현재 터미널 경로로 즉시 이동
- 새로운 SSH 및 SFTP 상태 메시지의 한국어·영어 번역 추가

### 배포 안정성

- Docker 이미지 빌드 전 builder 캐시 정리
- Docker 버전에 따른 캐시 정리 옵션 폴백 처리
- 새 컨테이너가 정상 실행된 후 dangling 이미지 정리
- 캐시 정리 전후 디스크 사용량 로그 추가
- 실행 중인 컨테이너 이미지와 named volume은 정리 대상에서 제외

---

## 보안 및 안정성

- 탭에서 생성된 tmux 세션 이름을 원격 셸 명령에 넣기 전에 정제
- `StrictHostKeyChecking=accept-new`를 사용해 SSH 호스트 키 검증 유지
- 배포 명령에서 안전하지 않은 SSH 파일 읽기 경로 제거
- 정상 종료와 SSH 연결 장애를 구분하는 종료 처리 추가
- 세션 키 정제, 재접속 정책, 타이머, 프롬프트 파싱, 스크롤백 경로 감지 테스트 추가

---

## 버그 수정

- SSH 터미널에서 Codex 및 전체 화면 터미널 애플리케이션이 제대로 표시되지 않던 문제 수정
- Ctrl+C, Tab 자동완성, 이스케이프 시퀀스, 붙여넣기 입력 처리 문제 수정
- `cd` 명령 후 SFTP 경로가 따라가지 않던 문제 수정
- 터미널 탭 간 SFTP 경로가 잘못 공유되던 문제 수정
- localStorage에 저장된 설정 때문에 SFTP 따라가기가 영구적으로 꺼지던 문제 수정
- 스크롤백이 쌓인 뒤 터미널 경로 감지가 멈추던 문제 수정
- 재접속 후 터미널 화면 상태가 정상적으로 복원되지 않던 문제 수정

---

## 알려진 제한 사항

- 영속 세션은 원격 서버에 tmux가 설치되어 있어야 하며, tmux가 없으면 일반 로그인 셸로 전환
- 자동 재접속은 SSH 서버가 다시 연결 가능한 상태가 되어야 동작
- 현재 작업 디렉터리는 지원되는 셸 프롬프트 또는 터미널 제목 형식에서 감지
- 모든 원격 셸 설정에 대한 완전한 E2E 검증은 추가 확인 필요

---

## 업그레이드 가이드

v1.0.4로 업그레이드할 때 다음 사항을 확인하세요.

- 기존 서버 레코드에 영속 세션 설정이 추가되며 기본값은 비활성화
- 서버 설정 화면에서 서버별 영속 세션 활성화 가능
- 별도 수동 데이터베이스 마이그레이션 불필요; migration 007 자동 실행
- 새로 여는 SFTP 패널은 터미널 경로 따라가기가 기본 활성화
- 새로운 PTY 및 재접속 동작을 사용하려면 기존 SSH 연결을 종료한 뒤 다시 연결

---

## 기술적 변경 사항

### 백엔드 (Rust / Tauri)

- 영속 세션 데이터베이스 migration 및 SSH 명령 파라미터 추가
- tmux 세션 래핑 및 재접속을 고려한 SSH 종료 이벤트 추가
- 세션 키 정제 및 PTY 생명주기 처리 추가

### 프론트엔드 (TypeScript / React)

- 테스트를 포함한 재접속 정책 및 스케줄러 모듈 추가
- 탭별 세션 상태와 터미널 크기 복원 추가
- 프롬프트 기반 현재 경로 감지 및 탭별 SFTP 따라가기 추가
- SSH 및 SFTP 상태 메시지의 한국어·영어 번역 추가

---

## 검증 결과

- TypeScript 컴파일 통과
- 프론트엔드 테스트 98개 통과
- 프론트엔드 프로덕션 빌드 통과

---

**전체 변경 내역:** v0.3.0...v1.0.4
